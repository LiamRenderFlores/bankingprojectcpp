// #include s to be filled in

// Class for Account Management
class Account {
public:
    void createAccount() {
        // Creates new account
    }

    void updateAccount() {
        // Updates account information
    }

    void deleteAccount() {
        // Deletes existing account
    }
};

// Class for Transaction History
class Transaction {
public:
    void makeTransaction() {
        // Updates transaction history and balance of account
    }

    void getTransactionHistory() {
        // Reads and displays transaction history
    }
};

// Class for User Authentication
class User {
public:
    bool authenticate() {
        // Checks if login information matches stored information and returns true if it does
    }
    void logout() {
        // Changes authenticate to false
    }
};

// Main function
int main() {
    // Allows user input and calls functions
    return 0;
}
